# IDE
/.idea/
/*.iml
/shelf/
# (workspace.xml is inside .idea, so this is redundant if .idea/ is ignored)
# /workspace.xml

# Build outputs
/target/
/out/
/build/
/artifacts/

# Runtime / local data
/filewatcher.db
*.log

# Secrets (keep these out of git)
/src/main/resources/credentials/*.json

# Misc IntelliJ tools
/httpRequests/
/mavenHomeManager.xml
/dataSources/
/dataSources.local.xml